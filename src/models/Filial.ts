import { RegionType } from "./Ctor";

export class Filial {
    id: number;
    name: string;
    description: string;
    code: RegionType;
    emails: string;
    phones: string;
    addresses: string;
}